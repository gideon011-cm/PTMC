local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Knit = require(ReplicatedStorage.Packages.Knit)

local RoundService = Knit.CreateService({
    Name = "RoundService"
})

function RoundService.Client:StartRound()
    
end

return RoundService